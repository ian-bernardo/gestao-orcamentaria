# Ambiente ativo: hml ou prod
ORACLE_ENV=hml

# URL base da API Oracle - Homologação (sem barra no final)
ORACLE_API_URL_HML=

# URL base da API Oracle - Produção (sem barra no final)
ORACLE_API_URL_PROD=
